# Ultramarathon-Time-Predictions
This repo is for a project by me and a classmate for DS675 at NJIT


## Setup
1. Install dependencies:
   pip install -r requirements.txt

2. Add Kaggle credentials:
  - Kaggles -> Settings -> API Tokens (Recommended) -> Generate New Token
  - use an .env file

3. Run the notebook
